server:
  port: 8081

spring:
  application:
    name: azure-demo-app

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
